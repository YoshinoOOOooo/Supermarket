spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:mysql://localhost:3306/supermarket_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${TEST_DB_USERNAME}
    password: ${TEST_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

app:
  security:
    admin:
      username: acceptance-admin
      password: acceptance-password
